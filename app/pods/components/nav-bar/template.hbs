<nav class="navbar navbar-full navbar-dark bg-inverse">
  {{#link-to "application" class="navbar-brand"}}
    Kube<span class="highlight">ADMIN</span>
  {{/link-to}}
  <ul class="nav navbar-nav">
    <li class="nav-item">
      {{link-to "Pods" "pods" currentNamespace class="nav-link"}}
    </li>
    <li class="nav-item">
      {{link-to "ReplicationControllers" "replication-controllers" currentNamespace class="nav-link"}}
    </li>
    <li class="nav-item">
      {{link-to "Services" "services" currentNamespace class="nav-link"}}
    </li>
  </ul>
  <form class="form-inline pull-xs-right">
    {{#power-select selected=currentNamespace options=namespaces onchange=(action "chooseNamespace") as |namespace|}}
      {{namespace}}
    {{/power-select}}
  </form>
</nav>
